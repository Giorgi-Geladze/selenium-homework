# Homework 2 & 3 - Test Automation

## Project Structure
- **POM (Page Object Model)** pattern used.
- **Parallel execution** enabled via `testng.xml`.
- **Allure Reporting** integrated for visual results.

## How to run the tests
1. Clone the repository.
2. Run tests using Maven:
   ```bash
   mvn clean test

